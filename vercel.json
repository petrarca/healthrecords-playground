{
  "version": 2,
  "buildCommand": "pnpm build && cd developer && pnpm build && cd .. && mkdir -p dist/developer && cp -r developer/dist/* dist/developer/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/developer/:path*", "destination": "/developer/:path*" }
  ],
  "github": {
    "silent": true,
    "autoJobCancelation": true
  }
}